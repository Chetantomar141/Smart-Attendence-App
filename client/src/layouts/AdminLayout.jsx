import React from 'react';
import DashboardLayout from './DashboardLayout';

const AdminLayout = () => {
  // Reusing the existing DashboardLayout which already contains Sidebar, Navbar and Outlet
  return <DashboardLayout />;
};

export default AdminLayout;
